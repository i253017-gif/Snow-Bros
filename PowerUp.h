#pragma once
#include <string>

// Power-up type constants used throughout the system
// Types: "speed", "snowball", "distance", "balloon"
// This header exists so other files can include it for the type strings.
